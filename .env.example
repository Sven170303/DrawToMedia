# =============================================================================
# Draw to Digital Media - Environment Variables (Local Development)
# =============================================================================
# Kopiere diese Datei zu .env.local für lokale Entwicklung mit Docker Desktop
#
# VERWENDUNG:
#   docker-compose -f docker-compose.local.yml --env-file .env.local up -d
#
# Für Coolify Deployment siehe:
# - .env.dev.example  (Development Environment)
# - .env.prod.example (Production Environment)
# =============================================================================

# -----------------------------------------------------------------------------
# Docker Local Development
# -----------------------------------------------------------------------------
# Port auf dem die App lokal erreichbar ist (Standard: 3002)
# Ändere diesen Wert falls Port 3002 bereits belegt ist
HOST_PORT=3002

# -----------------------------------------------------------------------------
# Node Environment
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# Supabase (Cloud-hosted)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://oiorfpkpqmakdakydkgl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# Stripe
# -----------------------------------------------------------------------------
# Publishable Key (Test: pk_test_xxx, Live: pk_live_xxx)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# -----------------------------------------------------------------------------
# App Configuration
# -----------------------------------------------------------------------------
# Muss mit HOST_PORT übereinstimmen!
NEXT_PUBLIC_APP_URL=http://localhost:3002

# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# SUPABASE EDGE FUNCTION SECRETS
# =============================================================================
# Diese Secrets werden NICHT im Frontend verwendet!
# Konfiguriere sie in: Supabase Dashboard > Edge Functions > Secrets
#
# Development (Test Mode):
#   STRIPE_SECRET_KEY=sk_test_xxx
#   STRIPE_WEBHOOK_SECRET=whsec_xxx (von Stripe CLI oder Dashboard)
#   FRONTEND_URL=http://localhost:3000
#
# Production (Live Mode):
#   STRIPE_SECRET_KEY=sk_live_xxx
#   STRIPE_WEBHOOK_SECRET=whsec_xxx (von Stripe Dashboard)
#   FRONTEND_URL=https://draw-to-digital.com
# =============================================================================
